---
name: no-provenance
description: >
  Planted defect — a plausible pattern with NO ledger provenance. A pattern distilled from nothing is
  an opinion, not precedent; the gate must REJECT it on provenance-present.
---
# no provenance

```json
{
  "cell": "pattern.system.no-provenance",
  "context": "a loop where workers repeatedly re-author a settled cell and clobber a hand-authored asset",
  "forces": "freedom to re-author versus protecting a validated asset",
  "solution_shape": "refuse to re-author a settled cell unless it is first un-shipped to regenerating",
  "consequences": "buys: a hand-authored asset is never silently stubbed over; costs: an explicit un-ship step",
  "provenance": {
    "refuted_when": "a settled cell is re-authored with no regenerating transition",
    "confirmed": true
  }
}
```
